scripts:
  server:
    - cd src && python manage.py runserver 8001
  makemigrations:
    - cd src && python manage.py makemigrations
  migrate:
    - cd src && python manage.py migrate
  curl_auth: | 
    curl -X POST -H "Content-Type: application/json" -d '{"username": "admin", 
    "password": "Perfect-password123"}' http://127.0.0.1:8001/api/token/pair
  curl_protect: | 
    curl -X GET -H "Authorization: Bearer <your_auth_access_token> http://127.0.0.1:8001/api/me
